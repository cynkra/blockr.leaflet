% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-pois.R
\name{snap_pois}
\alias{snap_pois}
\title{Snap points of interest onto a route}
\usage{
snap_pois(pois, route, tol_m = 1200)
}
\arguments{
\item{pois}{A data frame with \code{name}, \code{lat}, \code{lng}, and (optionally)
\code{type} columns, or \code{NULL}.}

\item{route}{A data frame with \code{lat} and \code{lng} columns (the track).}

\item{tol_m}{Maximum distance (m) from the track to keep a feature.
Default \code{1200}.}
}
\value{
A data frame with \code{lat}, \code{lng} (snapped to the track),
\code{label}, and \code{color}, or \code{NULL} if nothing matches.
}
\description{
Snaps each point of interest to the nearest point of a route track,
keeping only those within \code{tol_m}. Useful for overlaying named course
features on a GPS route regardless of which activity is shown: the
same gazetteer can be passed everywhere and only the features the
route actually passes are returned.
}
